import { Flame } from "lucide-react";
import classnames from "classnames";
import styles from "./rebuttal-card.module.css";

interface RebuttalCardProps {
  rebuttal: string;
  className?: string;
}

export function RebuttalCard({ rebuttal, className }: RebuttalCardProps) {
  return (
    <div className={classnames(styles.card, className)}>
      <div className={styles.header}>
        <div className={styles.headerIcon}>
          <Flame size={18} />
        </div>
        <h2 className={styles.title}>Savage Peer Review</h2>
        <span className={styles.subtitle}>Generated by Gemini 1.5 Pro</span>
      </div>
      <div className={styles.blockquote}>
        <p className={styles.body}>{rebuttal}</p>
      </div>
    </div>
  );
}
